{
  "name": "paper2project-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend start",
    "dev": "npm --prefix frontend run dev"
  }
}
